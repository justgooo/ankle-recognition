# ResNet18 + Slice Transformer/BiGRU + Attention Pooling + Cross-View Attention + Patient-Level Fusion 架构升级计划

## 先说人话：这份计划到底是在改什么

现在这套模型的做法可以粗略理解成：

```text
每张 CT 切片先交给 ResNet18 提特征
→ 同一个视角里的多张切片直接求平均
→ 3 个视角的结果直接拼起来
→ 交给一个小分类器判断“正常 / 异常”
```

这个做法的优点是简单、稳定、容易跑通；缺点是也比较“粗”：

- 它默认每张切片同样重要，但现实里关键病灶往往只出现在少数几张切片上。
- 它没有显式利用“切片是有顺序的”这个信息。
- 它把 3 个视角简单拼起来，但没有让 3 个视角互相“交流”。
- 分类头比较简单，多视角融合能力有限。

所以这份升级计划的核心目标只有一句话：

> 让模型从“粗暴平均 + 简单拼接”升级成“会挑重点、懂顺序、会看多视角关系、融合更细致”的版本。

---

## 读这份文档前，先看懂几个词

- `切片（slice）`：CT 不是一张图，而是一叠图。里面的每一张，就是一个切片。
- `视角（view）`：同一份 CT 可以从 3 个方向看，通常是轴状面、冠状面、矢状面。
- `特征（feature）`：模型从图像里提炼出来的一串数字，可以理解成“这张图的重要信息摘要”。
- `池化（pooling）`：把很多个特征压缩成一个特征。这里本质上就是“怎么把很多张切片总结成一个视角表示”。
- `Attention`：注意力机制。白话说，就是“模型自己学会把重点放在哪里”。
- `Fusion`：融合。白话说，就是“把多个来源的信息合在一起再做判断”。

---

## 当前架构 vs 目标架构：差距分析

下面逐模块对比现有 Baseline 与目标架构之间的差距，并说明每个改动到底在解决什么问题。

---

## 一、需要优化的 5 大模块

### 1. Slice-Level 序列建模：新增 Slice Transformer / BiGRU

这一步解决的问题是：

> 现在模型把一个视角下的多张切片直接平均了，但没有利用“这些切片前后是有顺序的”这个事实。

比如一组切片是从上到下排好的，前后相邻切片通常内容相近，这种顺序关系对判断病灶很有帮助。

| | 现状 | 目标 |
|---|---|---|
| 切片聚合方式 | `mean(dim=1)`，直接平均 | 先用 Transformer Encoder 或 BiGRU 建模切片顺序，再交给后续模块 |
| 位置信息 | 无 | 加入可学习的位置编码，让模型知道“第几张切片” |

**小白理解**：

- `Transformer` 可以理解成：让每张切片都能“看见”别的切片，再决定哪些信息重要。
- `BiGRU` 可以理解成：按顺序从前往后、再从后往前各看一遍，最后合并信息。
- 这一步不是直接分类，而是先把“切片之间的关系”建模好。

**需要改造的文件**：

#### [NEW] `src/slice_aggregator.py`

- 新增 `SliceTransformerAggregator`
  - 输入形状是 `(B, num_slices, 512)`
  - `B` 是 batch size，也就是一批有多少个病人
  - `num_slices` 是这个视角下有多少张切片
  - `512` 是 ResNet18 给每张切片提出来的特征维度
  - 加入 Learnable Position Embedding
    - 白话：告诉模型“这一张是第 1 张、第 2 张，还是第 10 张”
  - 经过 N 层 `TransformerEncoderLayer`
  - 输出仍然是 `(B, num_slices, 512)`，因为这里只是“增强每张切片的表示”，不是马上压缩成一个向量

- 新增 `SliceBiGRUAggregator`
  - 输入同样是 `(B, num_slices, 512)`
  - 用双向 GRU 处理切片序列
  - 双向隐藏状态拼接后，再线性映射回 512 维
  - 输出仍然保持 `(B, num_slices, 512)`

- 通过配置项选择方案：
  - `model.slice_aggregator: transformer | bigru | none`
  - `none` 表示不启用这一步，直接回退到原始做法

#### [MODIFY] `src/model.py`

- 在 `MultiViewEncoder.encode_views()` 里：
  - 现在是 `slice_features.mean(dim=1)`
  - 目标是先调用 `SliceAggregator`
- 这意味着：
  - 旧流程：切片特征 -> 直接平均
  - 新流程：切片特征 -> 序列建模 -> 再交给池化模块

**一句话总结**：

> 这一步是让模型“知道多张切片之间有先后顺序”，而不是把它们当成一堆互不相关的图。

---

### 2. Attention Pooling：替换 Mean Pooling

这一步解决的问题是：

> 现在所有切片一视同仁直接平均，但现实中并不是每张切片都一样重要。

比如 16 张切片里，可能只有 2 张真的包含关键病灶。如果直接平均，这 2 张的重要信息很容易被冲淡。

| | 现状 | 目标 |
|---|---|---|
| 切片 -> 视角特征 | `slice_features.mean(dim=1)` | Learnable Attention Pooling |

**小白理解**：

- `Mean Pooling`：每张切片都给一样的分数。
- `Attention Pooling`：让模型自己学“哪些切片更值得关注”。
- 最终还是压缩成一个视角向量，但不再是“平均主义”。

**需要改造的文件**：

#### [NEW] `src/attention_pooling.py`

- 新增 `AttentionPooling` 类
- 核心逻辑：

```python
# features: (B, S, d)
scores = Linear(d, 1)(features)        # 给每张切片打一个重要性分数
weights = softmax(scores, dim=1)       # 把分数转成权重，所有切片权重加起来等于 1
pooled  = (features * weights).sum(1)  # 按权重加权求和，得到最终视角特征
```

**上面这 3 行的人话解释**：

- 第 1 行：先让模型给每张切片打分
- 第 2 行：把分数变成占比
- 第 3 行：重要的切片多贡献一点，不重要的少贡献一点

- 可选增强：
  - Multi-Head Attention Pooling
  - 白话：不是只用一个“打分员”，而是多个“打分员”从不同角度给切片打分

#### [MODIFY] `src/model.py`

- 在 `encode_views()` 里，把 `mean(dim=1)` 换成 `AttentionPooling`
- 新增配置项：
  - `model.pooling_type: mean | attention`

**一句话总结**：

> 这一步是让模型自己挑重点切片，而不是默认每张切片同等重要。

---

### 3. Cross-View Attention：新增跨视角交互

这一步解决的问题是：

> 现在 3 个视角只是简单拼接，彼此之间没有交流。

但实际诊断里，一个视角看到的可疑结构，可能需要另一个视角补充确认。也就是说，3 个视角不是独立信息源，而是互相补充的。

| | 现状 | 目标 |
|---|---|---|
| 视角间关系 | `torch.cat(views, dim=1)` 简单拼接，或者各自分类后加权 | Cross-View Attention，让视角之间互相参考 |

**小白理解**：

- 以前是 3 个人各自说完话，把结论堆起来。
- 现在是 3 个人先开个会，互相听完对方说什么，再给出最终表达。

**需要改造的文件**：

#### [NEW] `src/cross_view_attention.py`

- 新增 `CrossViewAttention` 类
- 输入流程：
  - 原来 `encode_views(images)` 会得到 3 个 `(B, 512)` 的视角特征
  - 先 `stack` 成 `(B, 3, 512)`
  - 这个 `3` 就是 3 个视角
- 使用 `nn.MultiheadAttention(embed_dim=512, num_heads=8)` 做 self-attention
  - 白话：每个视角都能读取其他视角的信息
- 输出还是 `(B, 3, 512)`，再 reshape 成 `(B, 1536)` 给分类头

#### [MODIFY] `src/model.py`

- 在 `MultiViewCTClassifier.forward()` 里调整流程：

```python
# 之前：3 个视角直接拼接
image_feature = torch.cat(self.encode_views(images), dim=1)

# 之后：3 个视角先交互，再拼接
view_features = self.encode_views(images)      # 3 个 (B, 512)
stacked = torch.stack(view_features, dim=1)    # (B, 3, 512)
enhanced = self.cross_view_attention(stacked)  # (B, 3, 512)
image_feature = enhanced.reshape(B, -1)        # (B, 1536)
```

- 新增配置项：
  - `model.use_cross_view_attention: true | false`

**一句话总结**：

> 这一步是让 3 个视角“互相对照着看”，而不是各看各的。

---

### 4. Patient-Level Fusion：替换 / 增强分类头

这一步解决的问题是：

> 即使前面特征提得更好了，如果最后分类头太简单，也可能接不住这些更复杂的信息。

当前分类头只是一个简单的两层 MLP，能用，但表达能力有限。

| | 现状 | 目标 |
|---|---|---|
| 分类头结构 | `Linear -> ReLU -> Dropout -> Linear(2)` | Patient-Level Fusion Head，更精细地融合多视角信息 |

**小白理解**：

- 前面模块负责“收集信息”
- 分类头负责“最后拍板”
- 如果拍板的人太简单，前面收集来的复杂信息就可能白费了

**需要改造的文件**：

#### [MODIFY] `src/model.py`

- 新增 `PatientLevelFusionHead` 类
- 可以逐步加入这些能力：
  - `LayerNorm`
    - 白话：把数值分布先整理得更稳定一点，方便后面训练
  - 残差连接
    - 白话：让信息更容易保留下来，不容易在深层网络里丢失
  - 多层 MLP
    - 白话：让分类头有更强的表达能力
  - `Gated Fusion`
    - 白话：加一个“门”，控制哪些信息该放大，哪些该压低

参考逻辑：

```python
gate = sigmoid(Linear(1536, 1536)(fused))
fused = fused * gate
logits = classifier(fused)
```

**上面代码的人话解释**：

- 先生成一个和输入同大小的“开关向量”
- 每一维都会得到一个 0 到 1 之间的权重
- 重要的信息保留更多，不重要的信息被压小

- 当前直接写在 `nn.Sequential` 里的分类器，可以升级为独立模块
  - 好处是结构更清晰，也更方便以后继续加功能

**一句话总结**：

> 这一步是把“最后拍板”的分类器升级一下，让它更会融合多视角信息。

---

### 5. 训练策略、配置体系与数据增强

这一步解决的问题是：

> 模型结构改强了之后，训练方法也要跟上，否则容易训练不稳定、过拟合，或者训练效率不高。

| | 现状 | 目标 |
|---|---|---|
| 学习率调度 | 无（固定 lr） | `CosineAnnealingLR` 或 `ReduceLROnPlateau` |
| 数据增强 | 无 | `RandomFlip / RandomRotation / RandomCrop` |
| 早停 | 无 | `EarlyStopping` |
| 梯度裁剪 | 无 | `clip_grad_norm_` |
| 混合精度 | 无 | `autocast` + `GradScaler` |

**小白理解**：

- 学习率调度：相当于训练时“步子大小”会自动调整
- 数据增强：把训练图像稍微变一变，逼模型学得更稳，不要死记训练集
- 早停：发现验证集效果不再变好，就及时停，避免越训越过拟合
- 梯度裁剪：防止某次更新步子太大，把模型“带崩”
- 混合精度：更省显存，也通常更快

**需要改造的文件**：

#### [MODIFY] `train.py`

- 新增 `lr_scheduler` 配置与实例化
- 新增 `EarlyStopping` 逻辑
- 在 `train_one_epoch()` 中增加梯度裁剪
- 可选增加 AMP（自动混合精度）支持

#### [MODIFY] `src/dataset.py`

- 在 `__getitem__` 中增加可配置的数据增强管道
- 注意区分：
  - 训练集：可以做增强
  - 验证集 / 测试集：通常不要做随机增强

#### [MODIFY] `configs/*.yaml`

新增配置项建议如下：

```yaml
model:
  slice_aggregator: transformer  # 切片序列建模方式：transformer / bigru / none
  pooling_type: attention        # 切片汇总方式：attention / mean
  use_cross_view_attention: true # 是否启用 3 个视角之间的信息交互
  patient_fusion: gated          # 分类头类型：simple / gated

train:
  scheduler: cosine              # 学习率调度器：cosine / plateau / none
  early_stopping_patience: 10    # 验证集连续多少轮不提升就停止
  gradient_clip_norm: 1.0        # 梯度裁剪阈值
  use_amp: true                  # 是否使用混合精度训练

data:
  augmentation: true             # 是否启用训练集数据增强
```

**一句话总结**：

> 这一步不是直接提升模型结构，而是让训练过程更稳、更省资源、更不容易过拟合。

---

## 二、完整数据流对比

### 现有 Baseline

```text
CT Volume
→ 均匀采样切片
→ ResNet18 逐张提特征
→ mean pooling
→ cat 三视角
→ MLP
→ logits
```

### 目标架构

```text
CT Volume
→ 均匀采样切片 + 数据增强
→ ResNet18 逐张提特征
→ Slice Transformer / BiGRU 序列建模
→ Attention Pooling
→ Cross-View Attention（3 个视角先交互）
→ Patient-Level Gated Fusion Head
→ logits
```

**最简单的理解方式**：

- 旧版：先平均，再拼接，再分类
- 新版：先建模顺序，再挑重点，再让多视角互相交流，最后再分类

---

## 三、建议实施顺序

> 建议逐步升级、每步验证，不要一次把所有模块全改了。
> 原因很简单：一旦性能下降或代码报错，你能快速知道是哪个改动造成的。

| 步骤 | 改动 | 预期收益 | 风险 | 做完后你应该验证什么 |
|------|------|----------|------|----------------------|
| Step 1 | Attention Pooling 替换 Mean Pooling | 低风险、高回报，让模型自动关注关键切片 | 低 | 能正常训练；指标至少不明显变差 |
| Step 2 | Slice Transformer / BiGRU | 建模切片顺序关系 | 中 | 显存占用是否明显上升；指标是否提升 |
| Step 3 | Cross-View Attention | 让 3 个视角互相补充 | 中 | 前向 shape 是否正确；拼接前后的逻辑是否跑通 |
| Step 4 | Patient-Level Gated Fusion | 更精细地融合多视角信息 | 低 | 分类头是否稳定训练；是否比简单 MLP 更好 |
| Step 5 | 训练策略（Scheduler + AMP + EarlyStopping + 增强） | 训练更稳，减少过拟合 | 低 | 训练是否更平滑；验证集曲线是否更健康 |

**为什么这个顺序合理**：

- Step 1 改动最小，最容易先拿到收益
- Step 2 和 Step 3 是真正加“结构能力”的部分，但也更容易带来复杂度
- Step 4 放在后面，因为它依赖前面已经产出更好的融合特征
- Step 5 最后做，是因为训练策略通常是给已有结构“锦上添花”

---

## 四、新增文件总览

| 文件 | 说明 | 小白理解 |
|------|------|----------|
| `src/slice_aggregator.py` | Slice Transformer / BiGRU 序列建模 | 负责“理解切片前后顺序” |
| `src/attention_pooling.py` | Attention Pooling 模块 | 负责“从很多切片里挑重点” |
| `src/cross_view_attention.py` | Cross-View Attention 模块 | 负责“让 3 个视角互相交流” |

---

## 五、需要修改的文件总览

| 文件 | 修改内容 | 小白理解 |
|------|----------|----------|
| `src/model.py` | 集成所有新模块，重构 `MultiViewEncoder` 和分类器 | 主模型文件，核心逻辑基本都在这里 |
| `src/dataset.py` | 数据增强管道 | 决定训练时给模型喂什么样的数据 |
| `train.py` | Scheduler、EarlyStopping、梯度裁剪、AMP | 决定模型怎么训练 |
| `configs/*.yaml` | 新增所有配置项 | 让你不用改代码，只改配置就能切换实验方案 |

---

## 六、建议的落地方式

如果你想尽量降低出错概率，建议按下面方式执行：

1. 先只做 Step 1
2. 跑通训练和验证
3. 确认没有 shape 错误、loss 异常、指标大幅下降
4. 再做 Step 2
5. 后面的步骤也按同样方式推进

**不要做的事**：

- 不要一次性把 5 个模块全改完再跑
- 不要在没有对照实验的情况下判断“新模型一定更好”
- 不要忘记保留 `none / false / simple` 这些回退开关

因为只要你保留回退开关，就能快速回答这类关键问题：

- 这个提升到底是不是某个模块带来的？
- 某个模块值不值得保留？
- 是模型结构有问题，还是训练策略有问题？

---

## Verification Plan

### Automated Tests

1. 在每完成一个 Step 后，至少跑一次：

```powershell
python train.py --config configs/default.yaml
```

2. 检查 `runs/` 目录下生成的 `summary.json`
   - 确认训练正常结束
   - 确认指标有正常输出
   - 确认没有出现 NaN、shape mismatch、loss 不下降等明显异常

### Manual Verification

- 每个 Step 完成后，用 `default.yaml` 先跑一个“小规模验证版本”
  - 例如：假数据或小数据集 + 3 epochs
  - 目的不是追求最好指标，而是先确认逻辑无误
- 对比升级前后的 `val_auc` 和 `val_accuracy`
- 确认新增配置项设置成 `none / false / simple` 时，模型能退化回原始 Baseline 行为

---

## 最后一句总结

如果把现在的模型比作“把三份资料简单平均后交给一个人判断”，那么升级后的模型就是：

> 先按顺序整理资料 -> 挑出重点页 -> 让不同视角互相核对 -> 再由更强的决策模块给出结论。

这样做的目的，不是单纯“把模型变复杂”，而是让模型更像真正的诊断过程。

---

## 补充更新：改进项状态对照（截至 2026-04-05）

> 说明：本文件把 `implementation_plan.md.resolved` 里的目标，重新整理成两类：**已经改了的内容** 和 **尚未实现的目标**。  
> 依据来源：`backlog.md`、`program.md`、`results.tsv`、最近 git 提交记录。

## 当前最优基线

- **当前全局最佳 proxy**：`053cb3a`
- **方案**：View Reliability Gating（VRG，方案 2）
- **指标**：`no_miss_val_acc=0.798`，`no_miss_val_spe=0.620`，`val_AUC=0.960`
- **当前进行中主线**：Asymmetric Safety-Biased Fusion（ASF）定向 rerun，下一步是 `R6-06`

---

## 一、已经改了的内容

### 1. 方案 2：View Reliability Gating（VRG）——已实现，并且已成为当前最优

- 已在 `decision` 路径上加入**动态视角权重**，不再只依赖固定全局权重。
- 已完成定向 rerun：`R2-01 ~ R2-08`。
- 其中 `R2-08`（commit `053cb3a`）拿到当前全局最佳：
  - `no_miss_val_acc=0.798`
  - `no_miss_val_spe=0.620`
  - `val_AUC=0.960`
- 当前结论：**这是目前最成功的结构改动**，已经超过旧的经典 Decision Fusion 最优记录。

### 2. 方案 6：Asymmetric Safety-Biased Fusion（ASF）——已实现，但还没超过当前最优

- 已在 `MultiViewDecisionFusionClassifier.forward` 中加入**非对称融合**思路：
  - 正常分支走加权平均；
  - 异常分支走 `temperature` 控制的更激进融合。
- 已完成：`R6-01 ~ R6-05`。
- 当前 ASF 最好的一次是 `R6-04`（commit `a879bce`）：
  - `no_miss_val_acc=0.755`
  - `no_miss_val_spe=0.540`
  - `val_AUC=0.954`
- 当前结论：**ASF 已经改出来并跑通，但暂时没有打破 VRG 当前最优 0.798。**
- backlog 中仍有未跑完的 `R6-06 ~ R6-08`。

### 3. AttentionPooling 增强——已实现模块，但当前实验结果还不理想

- 已新增可学习切片聚合模块：`src/attention_pooling.py`。
- 已支持通过 YAML 配置切换 `use_attention_pooling: true`。
- 当前已完成：`AP-FF-01 ~ AP-FF-03`，均为 discard。
- 当前结论：**模块已经落地，但还没有证明它能优于现有最佳方案。**

### 4. Cross-View Attention——代码层面已具备实现，但不是当前主线

- 仓库中已有 `src/cross_view_attention.py`。
- 它对应“Attention Fusion / 跨视角交互”路线，说明**跨视角注意力这一类想法并不是完全停留在纸面上**。
- 当前结论：实现存在，但没有进入当前最高优先级的 autoresearch 主线。

### 5. 训练与评估流程强化——已完成并长期生效

这些不一定属于某个单独模型结构，但已经是项目里真正落地的“改进”：

- 使用**零漏诊阈值**做主模型选择标准；
- 固定 `train / val / test` 划分，避免实验漂移；
- 使用 `backlog.md + results.tsv` 做持续实验账本；
- 形成 proxy / formal 两档验证流程；
- 明确限制：**不改 `train.py`、不碰数据划分、不过度依赖测试集指标**。

这些约束和流程，本质上已经构成了项目的“研究工程化升级”。

---

## 二、尚未实现的目标

### 1. 方案 1：Hierarchical Hybrid Fusion —— 未实现

- 目标：把 Feature Fusion 和 Decision Fusion 两条分支放到同一个模型里，再用门控融合。
- 现状：在 `implementation_plan.md.resolved` 中有完整设想，**但仓库里还没有正式实现版本**。
- 原因：虽然代码量不算最大，但需要新建融合类，并重新理清训练入口下的兼容行为。

### 2. 方案 3：Slice-Level Cross-View Attention —— 未实现

- 目标：在切片级别而不是视角级别做跨视角交互。
- 现状：**尚未落地**。
- 原因：显存和过拟合风险都很高，尤其当前设备只有 8GB VRAM，小数据场景下风险偏大。

### 3. 方案 4：Multi-Granularity Feature Pyramid —— 未实现

- 目标：把 ResNet18 的多层特征一起拿来做融合，而不是只用最后一层 512 维特征。
- 现状：**尚未落地**。
- 原因：需要较大范围调整 `model.py` 中 encoder 的返回逻辑，工程改动面比 VRG / ASF 大很多。

### 4. 方案 5：Contrastive View Alignment —— 未实现（受规则约束）

- 目标：加辅助损失，让同一病人的多视角特征更一致。
- 现状：**还没有实现**。
- 主要阻塞：这一路线天然更依赖训练环节加辅助 loss，而项目规则明确禁止修改 `train.py`。
- 因此它目前属于：**理论上值得研究，但在当前协议下不适合优先推进**。

### 5. 论文就绪阶段任务 —— 尚未执行

这些不是“新结构”，但属于后续必须完成的目标：

- 多 seed 验证；
- Bootstrap 置信区间；
- 三种融合方式 formal 对比；
- 把当前最佳结构整理成更稳定的论文叙事。

---

## 三、当前还没做完但已经排进 backlog 的事项

| 事项 | 状态 | 备注 |
|---|---|---|
| `R6-06 ~ R6-08` | 未完成 | 继续验证 ASF 是否还有剩余空间 |
| `AP-FF-04 ~ AP-FF-08` | 未完成 | AttentionPooling 在 Feature Fusion 上还没跑完 |
| `AP-DF-01 ~ AP-DF-08` | 未完成 | AttentionPooling 在 Decision Fusion 上尚未系统开跑 |
| 阶段 3 formal 验证 | 未完成 | 论文就绪前必须补齐 |

---

## 四、一句话总结

- **已经改了并证明最有效的目标**：方案 2（VRG）。
- **已经改了但仍未打破最佳纪录的目标**：方案 6（ASF）、AttentionPooling。
- **还没实现的核心目标**：Hybrid Fusion、Slice-Level Cross-View、Multi-Granularity FPN、Contrastive Alignment。
- **当前最现实的推进路径**：先跑完 backlog 中已排定的 rerun / AttentionPooling 任务，再决定是否回到更高风险的新结构方案。
