# 足踝多视角 CT 二分类项目总结

本文按 **2026-04-03** 的代码、配置、`backlog.md`、`results.tsv` 与 `runs/` 产物状态重写前五章。
项目已经从“通用多视角 CT baseline”进入 **autoresearch 驱动的连续实验阶段**：
当前模型选择标准不是单纯追求 AUC，而是优先在验证集 **FN=0（零漏诊）** 约束下最大化准确率；
当前主线进入 **阶段 4B：Asymmetric Safety-Biased Fusion（ASF）**，但历史最佳 proxy 结果仍来自 **Decision Fusion 去掉 label smoothing** 的版本。

## 一、 项目文件结构

```text
足踝/
├── AGENTS.md                          # autoresearch Agent 行为约束与实验循环规则
├── backlog.md                         # 当前最优纪录、阶段优先级、Agent 状态
├── program.md                         # 完整实验协议、超时规则、资源保护规则
├── results.tsv                        # 实验结果总表（截至 ASF-05 共 67 条记录）
├── train.py                           # 训练主入口
├── configs/
│   ├── autoresearch_proxy.yaml        # 当前下一步：ASF-06 proxy（192px / 16 slices / 4 epochs）
│   ├── autoresearch_formal.yaml       # formal 确认基线（256px / 32 slices / 15 epochs）
│   ├── attention_fusion.yaml          # Attention Fusion 参考配置
│   ├── default.yaml                   # 通用 Feature Fusion 基础配置
│   ├── default_decision.yaml          # 通用 Decision Fusion 基础配置
│   ├── realdata.yaml                  # 真实数据配置
│   ├── paper_baseline.yaml            # 论文式对照实验配置
│   └── 若干历史 proxy / formal 配置     # 供回溯旧实验使用
├── src/
│   ├── model.py                       # Feature / Decision / Attention 三类模型定义
│   ├── attention_pooling.py           # 切片级注意力池化模块
│   ├── cross_view_attention.py        # 视角间 Transformer 交互模块
│   ├── dataset.py                     # PNG/JPG/BMP/DICOM/NIfTI 数据读取与采样
│   └── utils.py                       # 配置、指标、类别权重等工具函数
├── data/realdata/
│   ├── metadata.csv                   # 固定 train / val / test 划分主表
│   ├── CTyin/                         # 阴性病例目录
│   ├── CTyang/                        # 阳性病例目录
│   ├── metadata_template.csv          # 模板元数据
│   └── metadata_formal_26_10_2.csv    # 历史 formal 切分文件
├── runs/
│   ├── autoresearch_proxy/            # proxy 训练与阈值评估输出
│   └── autoresearch_formal/           # formal 训练与阈值评估输出
├── tools/                             # 阈值评估等工具（按协议只读）
├── 总结.md                             # 本文档
└── 改进.md                             # 较早期的结构升级方案文档
```

当前最关键的几个文件是：

- `backlog.md`：记录当前最佳 commit `7b184e0`，以及下一步实验 `ASF-06`。
- `configs/autoresearch_proxy.yaml`：已经切到 **ASF-06**，即 `fusion_type: decision`、`share_backbone: false`、`dropout: 0.2`、`augmentation: true`、4 epoch 快速 proxy。
- `configs/autoresearch_formal.yaml`：仍保留当前最稳妥的 **Feature Fusion formal 基线**，用于正式确认。
- `src/model.py`：目前 `decision` 路径已经改成 **ASF 实现**，不再是最早那版对称加权投票。
- `results.tsv`：是实验演化主账本，所有 keep / discard 判断都应回到这里与 `backlog.md` 一起看。

## 二、 使用的核心技术

### 1. 数据与任务定义

- 任务：**足踝多视角 CT 二分类**，输入为每位患者的 `axial / coronal / sagittal` 三个标准视角，输出“有病 / 无病”。
- 数据切分：`data/realdata/metadata.csv` 已固定为 **train 263 / val 94 / test 19**。
- 标签分布：总计 **376** 例，其中阴性 **199**、阳性 **177**；验证集为阴性 **50**、阳性 **44**；测试集为阴性 **10**、阳性 **9**。
- 数据格式：项目仍保留对 `png / jpg / jpeg / bmp / dcm` 切片输入以及 `nii / nii.gz` 体数据输入的兼容能力。

### 2. 模型骨干与三类融合路径

- 骨干网络：以 `ResNet18` 为主干提取每张切片的 512 维特征，支持 `share_backbone=true/false` 两种设置。
- 当前强势策略：**非共享 backbone（`share_backbone=false`）** 明显优于共享 backbone，已成为主线实验的默认前提。
- 三条融合路径仍然存在：
  - **Feature Fusion**：每个视角做切片聚合后，三路特征拼接，再接 MLP 分类头。
  - **Decision Fusion**：每个视角各自输出 logits，再做视角级融合；不过**当前代码里的 `decision` 实际上已经切换到 ASF 版本**。
  - **Attention Fusion**：使用 `Attention Pooling + Cross-View Attention`，作为独立的第三条路线保留在仓库中。

### 3. 当前 `decision` 路径的实际含义：ASF 版本

当前 `src/model.py` 里的 `MultiViewDecisionFusionClassifier` 已不是早期的“对称加权平均版” Decision Fusion，而是：

- **正常 logit**：仍用可学习视角权重做加权平均；
- **异常 logit**：改用 `temperature-scaled logsumexp`；
- 当前默认：`DEFAULT_TEMPERATURE = 0.5`；
- 设计目标：让“任一视角发现异常”时，最终输出更容易偏向异常，从而更贴近“**宁可误诊，不可漏诊**”的临床目标。

因此需要特别注意：

- **历史最佳 commit `7b184e0`** 对应的是“去掉 label smoothing 的经典 Decision Fusion”；
- **当前工作区代码** 对应的是“ASF 实验分支中的 decision 路径”；
- 如果要严格复现实验最优解，不能只看今天的 `model.py`，还要回看对应 commit 或恢复旧实现。

### 4. 训练与评估机制

训练端已经内置并验证可用的策略包括：

- `class_weight=true`：按训练集标签频次自动加权交叉熵；
- `augmentation=true`：随机翻转 + 小角度旋转，是当前最稳定的增益来源之一；
- `label_smoothing`：对 Feature Fusion 有帮助，但对经典 Decision Fusion 反而有害；
- `scheduler=cosine`、`gradient_clip_norm`、`early_stopping_patience`：均已做过 proxy 级验证，但暂未成为主线配置。

评估端当前最重要的规则是：

1. 训练完成后读取 `summary.json`；
2. 再运行阈值评估，生成 `threshold_eval.json`；
3. 用验证集**所有阳性样本中的最小阳性概率**作为零漏诊阈值；
4. 在这个阈值下计算 `no_miss_val_acc` 与 `no_miss_val_spe`；
5. **模型选择主指标是 `no_miss_val_acc`，AUC / F1 只是参考指标**；
6. **绝不使用测试集指标做模型选择**。

当前两档主力运行规格为：

- **proxy**：`192px / 16 slices / 4 epochs / batch_size=2`，用于快速筛选思路；
- **formal**：`256px / 32 slices / 15 epochs / batch_size=2`，用于正式确认表现。

## 三、 三种融合模式使用方法

### 1. 当前仓库里三种模式的定位

| 模式 | `fusion_type` | 当前代码状态 | 当前用途 |
|------|---------------|--------------|----------|
| Feature Fusion | `feature` | 稳定、可直接复现 | 作为 formal 基线与稳妥对照 |
| Decision / ASF | `decision` | **当前代码已切成 ASF 版** | 阶段 4B 主线实验 |
| Attention Fusion | `attention` | 代码可用，但当前不在主线 | 作为未来 formal 三路对比候选 |

### 2. 推荐的实际运行方式

**（1）跑当前 proxy 主线：**

```powershell
.\.venv\Scripts\python.exe train.py --config configs/autoresearch_proxy.yaml
.\.venv\Scripts\python.exe tools/evaluate_threshold.py --run_dir runs/autoresearch_proxy --config configs/autoresearch_proxy.yaml
```

这会使用当前 backlog 指向的下一步实验，也就是 **ASF-06**。

**（2）跑 formal 确认：**

```powershell
.\.venv\Scripts\python.exe train.py --config configs/autoresearch_formal.yaml
.\.venv\Scripts\python.exe tools/evaluate_threshold.py --run_dir runs/autoresearch_formal --config configs/autoresearch_formal.yaml
```

这仍然是当前最稳妥的 **Feature Fusion formal** 配置，而不是 ASF formal。

### 3. 当前各模式最值得记住的配置状态

- **Feature Fusion 当前最强 proxy**：`f10035f`
  - `share_backbone=false`
  - `augmentation=true`
  - `label_smoothing=0.05`
  - `dropout=0.3`
  - `no_miss_val_acc=0.776596`
  - `val_AUC=0.948182`

- **经典 Decision Fusion 历史最佳 proxy**：`7b184e0`
  - `share_backbone=false`
  - `augmentation=true`
  - `label_smoothing=0.0`
  - `lr=1e-4`
  - `dropout=0.3`
  - `no_miss_val_acc=0.787234`
  - `no_miss_val_spe=0.600000`
  - `val_AUC=0.940000`

- **Attention Fusion 当前状态**：仓库仍保留完整实现，但在当前 autoresearch 主线中不是优先实验方向，后续主要用于“论文就绪阶段”的正式三路对比。

### 4. 一个必须说明白的现实差异

如果今天直接把 `configs/autoresearch_proxy.yaml` 中的 `fusion_type` 设成 `decision` 并运行，得到的并不是历史上 `7b184e0` 那个“对称版 Decision Fusion”，而是**当前工作区中的 ASF 版本**。这也是为什么：

- `backlog.md` 里“当前最优纪录”与
- `src/model.py` 的“当前实现状态”

在语义上并不完全等价。前者是**历史最优结果**，后者是**正在探索的新结构代码**。

## 四、 当前研发路线（截至 2026-04-03）

当前路线已经不适合再用早期 `改进.md` 中的“Step 1 / Step 2 / Step 3”静态升级表来概括，更准确的说法应当是：

| 阶段 | 主题 | 状态 | 当前结论 |
|------|------|------|----------|
| 阶段 1 | Feature Fusion 超参优化 | 基本完成 | 已找到稳定最优区间，仅剩 formal-only cosine 作为低优先级补充验证 |
| 阶段 2 | Decision Fusion 对比 | 已完成主要搜索 | 产出了当前全局最佳 proxy 记录 `7b184e0` |
| 阶段 3 | 论文就绪验证 | 尚未开始 | 多 seed、Bootstrap、三种融合方式 formal 对比均待执行 |
| 阶段 4A | View Reliability Gating（VRG） | 已完成 | 10 个 proxy 全部 discard，未超过当前 best |
| 阶段 4B | Asymmetric Safety-Biased Fusion（ASF） | **进行中** | `ASF-01 ~ ASF-05` 全部 discard，`ASF-06` 已写入当前 proxy 配置 |

补充说明：

- backlog 记录的**上次实验**是 `ASF-05`，结果为 `discard`；
- 当前 backlog 记录的**下一步**是 `ASF-06: temperature=0.5 + dropout=0.2`；
- 当前阶段已经出现 **连续 5 个 discard**；
- 当前阶段累计 **0 个 proxy keep**；
- `model.py` 当前明确处于 **方案 6（ASF）** 的实现状态。

也就是说，项目已经完成了“从通用 baseline → Feature Fusion → Decision Fusion → VRG → ASF”的完整探索链条，正处在**结构创新路线是否能真正打破经典 Decision Fusion 上限**的关键节点。

## 五、 目前取得的成果

### 1. 当前最重要的几条记录

| 维度 | commit | 关键指标 | 结论 |
|------|--------|----------|------|
| 当前最佳零漏诊 proxy | `7b184e0` | `no_miss_val_acc=0.787234` / `spe=0.600000` / `threshold=0.155316` / `val_AUC=0.940000` | **全项目当前最佳**，来自经典 Decision Fusion 去掉 label smoothing |
| 当前最佳 Feature Fusion proxy | `f10035f` | `no_miss_val_acc=0.776596` / `val_AUC=0.948182` | 零漏诊准确率略低于最优，但 AUC 很强，是最稳的 Feature 基线 |
| 当前最佳 formal keep | `6589861` | `no_miss_val_acc=0.755319` / `spe=0.540000` / `threshold=0.272504` / `val_AUC=0.945909` | 当前 formal 参考解，说明 Feature Fusion 在正式训练下依然稳健 |
| 当前最高 proxy AUC | `a64cfa6` | `val_AUC=0.958636` / `no_miss_val_acc=0.755319` | 说明 **AUC 高 ≠ 零漏诊最优**，校准问题仍是核心矛盾 |
| 当前最新完成实验 | `9b04ab4` | `no_miss_val_acc=0.659574` / `spe=0.360000` / `val_AUC=0.929091` | `ASF-05` 仍未突破现有 best |

### 2. formal 结果目前能说明什么

当前 `runs/autoresearch_formal` 对应的正式结果显示：

- `best_val.accuracy = 0.829787`
- `best_val.f1 = 0.809524`
- `best_val.auc = 0.945909`
- 零漏诊阈值下：
  - `no_miss_val_acc = 0.755319`
  - `no_miss_val_spe = 0.540000`
  - `no_miss_threshold = 0.272504`
- 测试集（仅报告、不用于选模）：
  - `accuracy = 0.789474`
  - `auc = 0.866667`
  - `sensitivity = 0.888889`
  - `specificity = 0.700000`

这说明：**当前最稳的正式可复现基线仍然是 Feature Fusion 非共享 backbone + augmentation 路线**。

### 3. 截至目前已经得到的实验结论

1. **非共享 backbone 是正确方向。** 共享 backbone 阶段已经被系统性超越。
2. **数据增强是最大的稳定增益之一。** 从非共享 baseline 到 `augmentation=true`，零漏诊表现明显提升。
3. **Feature Fusion 与 Decision Fusion 的最优正则策略不同。**
   - Feature Fusion 中 `label_smoothing=0.05` 有帮助；
   - 经典 Decision Fusion 中去掉 label smoothing 反而最好。
4. **AUC 与零漏诊准确率不能混为一谈。** `a64cfa6` 的 AUC 最高，但最终仍不如 `7b184e0` 适合作为当前最优。
5. **VRG 与 ASF 目前都还没有打破经典 Decision Fusion 上限。**
   - VRG 的最佳信号出现在 `VRG-02`（高 AUC）与 `VRG-07`（较好校准），但组合后仍未超越 best；
   - ASF 到 `ASF-05` 为止仍主要暴露出“异常分支过于激进、阈值被压低、FP 偏多”的问题。

### 4. 目前项目所处的真实位置

如果只看仓库功能，这个项目早已不只是一个“多视角 CT 入门 baseline”；
如果只看结果，它也还没有进入“论文定稿前夜”。

更准确的描述是：

- **工程上**：已经具备完整的多视角训练、formal 确认、零漏诊阈值评估与实验账本管理能力；
- **研究上**：已经完成一轮系统的 Feature / Decision / VRG / ASF 探索；
- **结论上**：当前最值得信任的仍然是 `7b184e0` 代表的经典 Decision Fusion 最优 proxy，以及 `6589861` 代表的 Feature Fusion formal 稳定基线；
- **下一步**：继续验证 ASF 是否还有剩余空间；若继续失败，则回到 backlog 中定义好的 formal 触发条件与论文就绪验证阶段。

## 六、 最新阈值优化数据是如何计算出来的

> 说明：这一部分描述的是 **2026-03-28 最新真实数据实验** `runs/realdata_multiview_ct` 的阈值优化方法。  
> 这些数字**不是重新训练一个新模型得到的**，而是对已经训练好的 `best.pt` 在验证集上的输出概率做**后处理搜索**得到的。

### 1. 基础来源

最新这次实验使用的是：

- 配置：`configs/realdata.yaml`
- 最优权重：`runs/realdata_multiview_ct/best.pt`
- 验证集大小：94 例
- 测试集大小：19 例

训练阶段本身仍然使用交叉熵损失，模型选择优先依据验证集 `AUC`。  
当前 `train.py` 在评估时默认做法是：

1. 对 logits 做 `softmax`
2. 取正类概率最大的类别作为预测结果

在二分类场景中，这等价于默认使用 **0.5 阈值**。

### 2. 阈值优化所用算法

阈值优化的本质是：  
先让模型对每个样本输出一个“异常概率”，再遍历不同阈值，比较哪个阈值下指标最好。

#### 第一步：计算每个样本的正类概率

对每个验证样本，模型输出两个 logit：`[logit_0, logit_1]`，再通过 Softmax 转为概率：

```text
p_i = exp(logit_1) / (exp(logit_0) + exp(logit_1))
```

其中：

- `p_i` 表示第 `i` 个样本属于“异常/阳性”的概率

#### 第二步：遍历候选阈值

对阈值 `t` 进行扫描（使用验证集真实输出概率边界 + 0.01 步长网格）：

```text
ŷ_i(t) = 1, if p_i >= t
ŷ_i(t) = 0, if p_i <  t
```

也就是：

- 当正类概率 `p_i` 大于等于阈值 `t`，预测为异常
- 否则预测为正常

#### 第三步：对每个阈值计算混淆矩阵

对每个阈值，都计算：

- `TP`：真实异常，预测也异常
- `FP`：真实正常，却预测为异常
- `TN`：真实正常，预测也正常
- `FN`：真实异常，却预测为正常

然后据此计算常用指标：

```text
Accuracy    = (TP + TN) / N
Sensitivity = TP / (TP + FN)
Specificity = TN / (TN + FP)
F1          = 2 * Precision * Recall / (Precision + Recall)
BalAcc      = (Sensitivity + Specificity) / 2
Youden J    = Sensitivity + Specificity - 1
```

### 3. 为什么会得到 0.35、0.38、0.42 这几个阈值

阈值搜索后，不同目标会对应不同“最优阈值”：

#### 方案 A：追求整体平衡

在验证集上同时兼顾：

- `F1`
- 平衡准确率 `Balanced Accuracy`
- `Youden J`

得到的最优阈值约为：

- **0.346987**

对应验证集表现为：

- Accuracy = **0.8830**
- F1 = **0.8736**
- Sensitivity = **0.8636**
- Specificity = **0.9000**

#### 方案 B：更重视高特异度

如果约束 **Specificity ≥ 0.95**，再在满足条件的阈值中尽量提高灵敏度，则得到：

- **0.423403**

对应验证集表现为：

- Accuracy = **0.8830**
- F1 = **0.8642**
- Sensitivity = **0.7955**
- Specificity = **0.9600**

#### 方案 C：更重视高召回

如果约束 **Sensitivity ≥ 0.90**，再在满足条件的阈值中尽量提高特异度，则得到：

- **0.305886**

对应验证集表现为：

- Sensitivity = **0.9091**
- Specificity = **0.8000**

### 4. 为什么最终更推荐用 0.38，而不是死用 0.346987

`0.346987` 是基于当前验证集概率边界得到的一个**精确最优点**，但它可能对这一折验证集有一定“贴边拟合”。

因此又做了 **Bootstrap 重采样稳定性分析**：

- 对验证集做 1000 次有放回抽样
- 每次重新寻找 F1 最优阈值
- 统计阈值分布范围

结果显示：

- 阈值中位数约为 **0.381885**
- 25% ~ 75% 分位区间约为 **0.346987 ~ 0.423403**

这说明对于当前模型，真正稳定的实用阈值区间更接近：

- **0.35 ~ 0.42**

所以工程上更推荐把默认部署阈值写成：

- **0.38**（平衡型默认值）

而不是写成过于精细、容易过拟合单次验证集的 `0.346987`。

### 5. 阈值优化的结论

对于最新这次 `realdata_multiview_ct` 结果，可以把阈值策略概括为：

- **默认 0.5**：更保守，特异度高，但漏检更多
- **0.38 左右**：综合最平衡，适合作为默认部署阈值
- **0.31 左右**：适合筛查任务，尽量少漏诊
- **0.42 左右**：适合更重视误报控制的场景

### 6. 方法学注意事项

1. **阈值一定要在验证集上调，不能在测试集上调。**
2. 测试集只用于最后一次固定阈值后的客观评估。
3. 由于本项目当前测试集只有 19 例，单次测试结果波动会比较大。
4. 如果后续要正式部署，建议进一步使用：
   - K 折交叉验证
   - Bootstrap 置信区间
   - Temperature Scaling / Platt Scaling / Isotonic Regression 做概率校准

总结一句话：

> 最新阈值优化数据的计算方法，本质上是 **“对 best.pt 在验证集上的正类概率做网格搜索 + 混淆矩阵指标评估 + Bootstrap 稳定性分析”**，而不是重新训练出一个新的阈值模型。


## 七、 2026-03-30 新增：四次实验的“验证集不漏诊阈值”调优结果

> 说明：这一部分把最近 4 次真实数据实验都按统一规则做了一次阈值调优。  
> 规则是：**只在验证集调阈值**，并取 **在验证集上 FN=0 / Sensitivity=1.0** 的最高阈值；测试集只用于固定阈值后的客观评估，不参与调参。  
> 预测规则统一为：`ŷ = 1 if p >= t else 0`。在这个规则下，“最高不漏诊阈值”等价于**验证集所有阳性样本中最小的正类概率**。  
> 本次统一结果已另存为：`runs/threshold_no_miss_summary.json`。

### 1. 核心结果总表

| 日期 | 实验目录 | 划分方式 | 融合方式 | 验证集不漏诊阈值 | 验证集（Acc / F1 / Sen / Spe / FN） | 测试集（Acc / F1 / Sen / Spe / FN） | 简要结论 |
|------|----------|----------|----------|------------------|--------------------------------------|--------------------------------------|----------|
| 2026-03-28 | `runs/realdata_multiview_ct` | 固定 split（263 / 94 / 19） | 特征融合 | **0.2567** | **0.713 / 0.765 / 1.000 / 0.460 / 0** | **0.632 / 0.632 / 0.667 / 0.600 / 3** | 若把目标从“平衡”改成“验证集绝不漏诊”，阈值需从平衡型方案进一步下调；但测试集仍有 3 例漏诊。 |
| 2026-03-29 | `runs/realdata_decision_full` | 固定 split（263 / 94 / 19） | 决策融合 | **0.1646** | **0.798 / 0.822 / 1.000 / 0.620 / 0** | **0.789 / 0.800 / 0.889 / 0.700 / 1** | 在固定 split 的三组实验里，若以“召回优先/尽量不漏诊”为目标，它是当前综合折中最好的版本。 |
| 2026-03-29 | `runs/formal_attention_full_ratio_26_10_2` | 重新分层切分（257 / 99 / 20） | 注意力融合 | **0.2705** | **0.505 / 0.657 / 1.000 / 0.058 / 0** | **0.500 / 0.643 / 1.000 / 0.091 / 0** | 虽然验证/测试都做到了 0 漏诊，但代价是特异度极低；且切分不同，仍不宜与固定-split 主表做严格横比。 |
| 2026-03-30 | `runs/realdata_attention_fixedsplit_nonshared` | 固定 split（263 / 94 / 19） | 注意力融合 | **0.1033** | **0.585 / 0.693 / 1.000 / 0.220 / 0** | **0.579 / 0.667 / 0.889 / 0.300 / 1** | 为了在验证集上消除 FN，阈值必须压得很低；在公平 fixed-split 对比下，当前 recall-first 表现仍未超过决策融合。 |

补充说明：

- 表中验证集与测试集指标顺序统一为 **Accuracy / F1 / Sensitivity / Specificity / FN**。
- **AUC 不受阈值变化影响**，因此这里不重复抄写；对应 AUC 仍与各实验 `summary.json` 中原始结果一致。
- 所谓“**不漏诊阈值**”指的是：**验证集上不漏诊**，并不意味着对未来未见样本也必然 0 漏诊。

### 2. 与此前“平衡阈值”结论的关系

前一节里给 `realdata_multiview_ct` 推荐的 **0.38 左右**，本质上是一个**平衡型部署阈值**：兼顾 F1、BalAcc、Youden J 和 Bootstrap 稳定性。  
而本节新增的 **0.2567**，对应的是一个更激进的**验证集零漏诊阈值**：

- **0.38 左右**：更适合“综合表现尽量均衡”的场景
- **0.2567**：更适合“宁可多报，也尽量不漏诊”的场景

这也说明：**“最佳平衡阈值”** 和 **“零漏诊阈值”** 不是同一个概念，必须先明确临床目标，再决定该采用哪一类阈值。

### 3. 当前可得出的结论

1. **如果目标改成“召回优先 / 尽量不漏诊”**，那么在固定 split 的三条可严格比较的实验里，  
   当前最佳折中是 **`realdata_decision_full` + 阈值 0.1646**：
   - 验证集：Sensitivity = **1.000**, Specificity = **0.620**
   - 测试集：Sensitivity = **0.889**, Specificity = **0.700**, F1 = **0.800**

2. **两个 attention 版本都需要非常低的阈值才能做到验证集 0 漏诊**，这说明它们的阳性样本概率下界更低；  
   尤其 `realdata_attention_fixedsplit_nonshared` 的不漏诊阈值只有 **0.1033**，提示其概率分布在 recall-first 使用场景下仍不够理想。

3. **“验证集 0 漏诊”不等于“测试集 0 漏诊”**。  
   除了 `formal_attention_full_ratio_26_10_2` 这组不同切分实验外，其余三组在测试集上仍然存在 FN，说明当前数据规模下单次切分波动依然明显。

### 4. 方法学注意事项（新增）

1. 如果后续论文主张改为“**临床筛查优先，不漏诊优先**”，建议把“验证集约束 Sensitivity = 1.0 的阈值选择方法”单独写进方法部分。
2. 该阈值必须始终只在验证集上确定，**不能在测试集上反推**。
3. 由于当前测试集只有 **19 / 20 例**，即使测试集 FN 从 4 例降到 1 例，也仍然可能有较大统计波动。
4. 若后续要把 recall-first 结论写得更稳，建议补充：
   - 多 seed 重复实验
   - K 折交叉验证
   - Bootstrap 置信区间
   - 概率校准（Temperature Scaling / Platt Scaling / Isotonic Regression）

## 八、评估指标详解

> 本节用具体数值示例帮助理解各项指标的含义与区别。

### 1. 模型如何输出"预测结果"

模型看完一组 CT 图像后，并不直接说"有病"或"没病"，而是输出一个 **0~1 之间的概率值**（正类概率），例如：

| 患者 | 真实情况 | 模型输出的概率 |
|------|----------|:-:|
| 张三 | 有病 | 0.92 |
| 李四 | 有病 | 0.78 |
| 王五 | 有病 | 0.31 |
| 赵六 | 有病 | 0.18 |
| 孙七 | 没病 | 0.12 |
| 周八 | 没病 | 0.25 |
| 吴九 | 没病 | 0.08 |
| 郑十 | 没病 | 0.45 |

模型只告诉你"我觉得这个人有病的概率是多少"。要做出最终的"有病/没病"判断，还需要设定一个**阈值**：概率 >= 阈值 -> 判有病，概率 < 阈值 -> 判没病。

---

### 2. Accuracy（准确率）— 需要先选阈值

准确率 = **判断正确的人数 / 总人数**。

但准确率**取决于你选什么阈值**。同一个模型，换不同阈值，准确率完全不一样：

**用上面的例子，阈值 = 0.5 时：**

| 患者 | 真实 | 概率 | 判定（>=0.5则有病）| 对错 |
|------|------|:----:|:---:|:---:|
| 张三 | 有病 | 0.92 | 有病 | 对 |
| 李四 | 有病 | 0.78 | 有病 | 对 |
| 王五 | 有病 | 0.31 | 没病 | 错-漏诊 |
| 赵六 | 有病 | 0.18 | 没病 | 错-漏诊 |
| 孙七 | 没病 | 0.12 | 没病 | 对 |
| 周八 | 没病 | 0.25 | 没病 | 对 |
| 吴九 | 没病 | 0.08 | 没病 | 对 |
| 郑十 | 没病 | 0.45 | 没病 | 对 |

正确 6 个 / 总共 8 个，**Accuracy = 75%**。但漏诊了 2 个病人。

**阈值 = 0.15 时：**

| 患者 | 真实 | 概率 | 判定（>=0.15则有病）| 对错 |
|------|------|:----:|:---:|:---:|
| 张三 | 有病 | 0.92 | 有病 | 对 |
| 李四 | 有病 | 0.78 | 有病 | 对 |
| 王五 | 有病 | 0.31 | 有病 | 对 |
| 赵六 | 有病 | 0.18 | 有病 | 对 |
| 孙七 | 没病 | 0.12 | 没病 | 对 |
| 周八 | 没病 | 0.25 | 有病 | 错-误诊 |
| 吴九 | 没病 | 0.08 | 没病 | 对 |
| 郑十 | 没病 | 0.45 | 有病 | 错-误诊 |

正确 6 个 / 总共 8 个，**Accuracy = 75%**。0 漏诊但 2 个误诊。

**结论**：准确率相同（都是 75%），但错误的类型完全不同。这就是为什么光看准确率不够。

---

### 3. AUC — 不需要选阈值，衡量"排序能力"

AUC 回答的问题是：**模型给有病的人打的分，是否普遍高于没病的人？**

具体做法：从有病的人里随机取一个，从没病的人里随机取一个，看模型给有病的那个打的分是否更高。对**所有可能的配对**都做这个比较，正确的比例就是 AUC。

**用上面的例子：**
- 有病的人概率：0.92, 0.78, 0.31, 0.18
- 没病的人概率：0.12, 0.25, 0.08, 0.45

所有 4x4 = 16 个配对中：

| 有病(行) vs 没病(列) | 0.12 | 0.25 | 0.08 | 0.45 |
|--------|:----:|:----:|:----:|:----:|
| **0.92** | 对 | 对 | 对 | 对 |
| **0.78** | 对 | 对 | 对 | 对 |
| **0.31** | 对 | 对 | 对 | 错 |
| **0.18** | 对 | 错 | 对 | 错 |

正确 13 个 / 总共 16 个，**AUC = 0.8125**

注意：**计算 AUC 的全程不需要设定任何阈值**。它衡量的纯粹是模型区分两类人的"排序能力"。

---

### 4. AUC 高但 Accuracy 不高，为什么？

关键区别总结：

| | AUC | Accuracy |
|--|-----|----------|
| 需要选阈值吗？ | 不需要 | 需要 |
| 衡量什么？ | 有病和没病的人**分数排序**是否正确 | 在某个阈值下**判断结果**是否正确 |
| 能分开就一定准吗？ | 能分开 ≠ 能分得干净 | 取决于阈值选得好不好 |

**"能分开有病无病不就能判断对错吗？"—— 不一定。**

上面的例子中：
- 赵六（有病，概率 0.18）和周八（没病，概率 0.25）的分数**交叉了**
- 模型虽然总体上给有病的人打分更高（AUC=0.81），但有个别有病的人分数比没病的还低
- 无论你把阈值设在哪里，都没法**同时**把赵六分对、又把周八分对

这就是 AUC 和 Accuracy 脱钩的原因：**AUC 看整体排序能力，Accuracy 看具体的判定线效果**。

在本项目中：
- AUC 最高的模型（0.959）：整体排序非常好，但有个别病人的概率分数很低，零漏诊时要把阈值压得特别低 -> 很多健康人被误报 -> 零漏诊 Accuracy 只有 0.755
- 零漏诊 Accuracy 最高的模型（0.787，AUC=0.940）：整体排序稍差，但**所有病人的分数都不太低**，阈值不用压太低 -> 误报少 -> 准确率更高

---

### 5. 零漏诊阈值是怎么确定的

**目标：保证验证集上所有有病的人都不漏掉（FN = 0，Sensitivity = 100%）。**

确定方法非常直观——用上面的例子演示：

**第一步**：拿训练好的模型，对验证集每个人算出正类概率：

| 患者 | 真实情况 | 概率 |
|------|----------|:----:|
| 张三 | **有病** | **0.92** |
| 李四 | **有病** | **0.78** |
| 王五 | **有病** | **0.31** |
| 赵六 | **有病** | **0.18** |
| 孙七 | 没病 | 0.12 |
| 周八 | 没病 | 0.25 |
| 吴九 | 没病 | 0.08 |
| 郑十 | 没病 | 0.45 |

**第二步**：找到**有病的人里面，概率最低的那个** -> 赵六 = 0.18

**第三步**：阈值设为 **<= 0.18**（通常就直接取这个最小值）

这样，只要概率 >= 0.18 就判有病：
- 张三 0.92 >= 0.18 -> 有病（没漏掉）
- 李四 0.78 >= 0.18 -> 有病（没漏掉）
- 王五 0.31 >= 0.18 -> 有病（没漏掉）
- 赵六 0.18 >= 0.18 -> 有病（没漏掉）<- 刚好卡在线上
- 孙七 0.12 < 0.18 -> 没病（正确）
- **周八 0.25 >= 0.18 -> 有病（误诊！他其实没病）**
- 吴九 0.08 < 0.18 -> 没病（正确）
- **郑十 0.45 >= 0.18 -> 有病（误诊！他其实没病）**

零漏诊达成，但代价是 2 个健康人被误诊。

**本质就是**：零漏诊阈值 = 验证集**所有阳性样本中，模型给出的最低概率**。如果有一个病人概率特别低（说明模型对他"不太确定"），阈值就要跟着降得很低，导致更多健康人被误报。

这也解释了为什么我们的当前最优模型零漏诊阈值只有 **0.155**——验证集 44 个病人中，有一个病人模型只给了他 15.5% 的概率，但他确实有病，所以阈值只能降到这么低。

---

### 6. 其他评估指标

| 指标 | 中文名 | 公式 | 含义 |
|------|--------|------|------|
| **Sensitivity** | 灵敏度/召回率 | 查出的病人 / 实际病人总数 | 有病的人里，查出来多少（越高越不漏诊） |
| **Specificity** | 特异度 | 排除的健康人 / 实际健康人总数 | 没病的人里，放行多少（越高越不误诊） |
| **F1 Score** | F1 值 | 灵敏度和精确度的调和均值 | 综合"查得全"和"查得准"的平衡分 |
| **FN** | 假阴性 | — | 有病但模型判成没病的人数（= 漏诊数） |
| **FP** | 假阳性 | — | 没病但模型判成有病的人数（= 误诊数） |

**灵敏度和特异度是一对此消彼长的指标**：为了不漏诊（灵敏度上升），就要降低阈值，那更多健康人会被误报（特异度下降）。"零漏诊"的意思就是灵敏度 = 100%，此时特异度能保持多高就看模型的区分能力了。

---

## 九、关键训练参数详解

> 这些参数控制模型"怎么学习"，就像调节学生的学习方法。

### 1. 模型结构参数（"模型长什么样"）

#### fusion_type — 融合方式

三个视角的 CT 各自提取了特征后，怎么合并成最终判断：

| 融合方式 | 比喻 | 做法 |
|----------|------|------|
| **feature**（特征融合） | 三个医生先把笔记合成一份，再一起做决定 | 把三个视角的特征拼在一起，统一送入分类器 |
| **decision**（决策融合） | 三个医生各自独立给出意见，然后投票 | 每个视角独立做判断，加权平均 |
| **attention**（注意力融合） | 三个医生先互相讨论，再各自给意见 | 三个视角互相交换信息，再分类 |

#### share_backbone — 是否共享骨干网络

骨干网络（ResNet18）负责"看图提取特征"。

| 设置 | 比喻 |
|------|------|
| true | 一个医生轮流看三个方向（共用一套经验） |
| false | 三个医生各看一个方向（各有独立经验） <- **效果更好** |

#### fusion_hidden_dim — 分类头中间层的宽度

模型拿到三个视角的特征后，要输出「有病/没病」的判断。这个决定分两步完成：

```
三视角特征拼接         中间层（提炼压缩）        最终输出
1536 个数字    -->    256 个数字         -->    2 个数字（有病概率、没病概率）
                      ↑ 这里有多少个数字 = fusion_hidden_dim
```

**类比**：想象你要写一份会诊结论：
- **输入**：三个科室各给你一份 512 项检查数据，总共 **1536** 项原始数据
- **中间层**（fusion_hidden_dim = 256）：你把 1536 项数据 **提炼压缩** 成 **256** 条关键发现
- **输出**：基于这 256 条关键发现，做出「有病 / 没病」的最终结论

这个中间层需要多大？
- **128**：压缩太狠，关键信息丢了，判断不准（欠拟合）
- **256**（默认）：压缩适中，保留关键信息又不冗余（最优）
- **384**：保留太多，把噪音也当成了关键发现，模型死记硬背训练数据（过拟合）

#### cross_view_heads — 跨视角注意力头数（仅 Attention Fusion）

三个视角互相"讨论"时，开几路并行讨论。每路关注不同方面（骨密度、关节间隙、软组织等）。

#### cross_view_layers — 跨视角注意力层数（仅 Attention Fusion）

讨论几轮。层数越多信息交换越充分，但小数据上容易过拟合。

---

### 2. 训练策略参数（"怎么学习"）

#### lr（学习率）— 每一步学多大

模型每次更新参数时的调整幅度。就像蒙眼下山找最低点时的步长：

| 值 | 效果 |
|----|------|
| 3e-4（0.0003） | 大步走，快但容易跳过最低点 |
| **1e-4（0.0001）** | 中等，**多数实验最优** |
| 7e-5（0.00007） | 较小步子 |
| 5e-5（0.00005） | 小碎步，更精确但可能走不到位 |

#### dropout（丢弃率）— 随机关闭部分神经元

训练时随机"关掉"一定比例的神经元，防止模型死记硬背（过拟合）。就像考试时随机禁止部分学生参加，逼其他人独立思考。

| 值 | 效果 |
|----|------|
| 0.2 | 关 20%，正则化较弱 |
| **0.3** | 关 30%，**几乎总是最优** |
| 0.4 | 关 40%，过度，效果反而变差 |

#### label_smoothing / ls（标签平滑）— 别太绝对

正常训练时，模型的学习目标是"有病=100%，没病=0%"。加了 ls=0.05 后变成"有病=97.5%，没病=2.5%"，让模型不那么"死板自信"。

**注意**：这个参数的效果因融合方式而异——Feature Fusion 中有益，Decision Fusion 中反而有害。

#### augmentation / aug（数据增强）— 人为增加数据多样性

训练时对图片做随机翻转和旋转（正负10度），相当于把一张图变成好几张略有不同的图。是本项目中**提升最大的单一改进**。

#### weight_decay / wd（权重衰减）— 防止参数太大

每次更新时让所有参数略微缩小，防止模型走极端。默认 1e-4，增大到 5e-4 时效果反而变差。

#### gradient_clip_norm（梯度裁剪）— 限速器

如果某次更新幅度过大（梯度爆炸），强制限制在一个范围内。本项目中无明显帮助。

#### scheduler（学习率调度器）— 控制学习节奏

| 类型 | 做法 |
|------|------|
| none（默认） | 学习率始终不变 |
| cosine | 学习率按余弦曲线从初始值逐渐降低到接近 0（先快后慢） |

#### epochs（训练轮数）— 数据看几遍

- proxy 实验：4 轮，约 30 分钟（快速筛选用）
- formal 实验：15 轮，约 2 小时（正式确认用）

#### batch_size（批大小）— 一次看几个样本

每次同时处理多少个样本再更新参数。受 GPU 显存限制，当前用 batch_size = 2。

#### early_stopping_patience（早停）— 连续没进步就停

验证集成绩连续 N 轮没提升就提前终止训练，防止过拟合。在 4 轮的 proxy 实验中基本无效。
