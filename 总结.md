# 足踝多视角 CT 二分类项目总结

这个足踝多视角 CT 二分类项目是一个针对医学影像深度学习的**最小可运行框架 (Baseline)**，并已完成第一阶段架构升级（注意力融合）。根据它的架构与文档，以下是它所使用的主要技术和目前取得的成果：

## 一、 项目文件结构

```
足踝/
├── train.py                        # 训练主入口（支持 feature / decision / attention 三种融合模式）
├── configs/
│   ├── default.yaml                # 默认配置（特征融合，轻量/CPU 可跑）
│   ├── default_decision.yaml       # 决策融合配置
│   ├── paper_baseline.yaml         # 对照论文实验配置
│   ├── realdata.yaml               # 真实大尺寸数据配置
│   └── attention_fusion.yaml       # 注意力融合配置（Attention Pooling + Cross-View Attention）
├── src/
│   ├── model.py                    # 模型定义（MultiViewCTClassifier / DecisionFusion / AttentionClassifier）
│   ├── dataset.py                  # 数据集加载与预处理（支持 PNG/JPG/DICOM/NIfTI）
│   ├── utils.py                    # 工具函数（设备选择、指标计算、配置加载等）
│   ├── attention_pooling.py        # [新增] 可学习注意力池化模块
│   └── cross_view_attention.py     # [新增] 跨视角 Transformer 自注意力模块
├── 总结.md                         # 本文件 — 项目技术总结
└── 改进.md                         # 架构升级计划（5 大模块改进路线）
```

## 二、 使用的核心技术

### 1. 基础框架与工具栈
*   **深度学习框架**：基于 `PyTorch` (`torch`, `torchvision`) 进行模型搭建与训练。
*   **数据处理与指标**：使用 `numpy`、`pandas` 进行数据分析与组织，`scikit-learn` 用于计算 AUC/Accuracy 等评价指标。
*   **医学影像解析**：使用 `pydicom` 解析 DICOM 切片格式数据，以及自定义代码自动解析 `nii / nii.gz` 等 3D 体数据格式。

### 2. 核心网络架构与算法
*   **2.5D 多视角分类架构**：采用了提取 `axial`（轴状面）、`coronal`（冠状面）、`sagittal`（矢状面）三个标准视角的 2D 切片序列的做法，而非计算开销极大的完整 3D 网络。
*   **骨干网络 (Backbone)**：采用轻量且经典的 `ResNet18` 作为基础特征提取器（可加载 ImageNet 预训练权重）。输入通道已从 3（RGB）改为 1（灰度 CT），预训练权重通过通道均值迁移。
*   **多视角融合策略**，包含三种模式：
    *   **特征融合 (Feature Fusion)**：在每个视角抽取固定数量的切片经过特征提取与**平均池化**后，将三视角的特征拼接 (Concat → 1536 维)，再送入分类头（Linear → ReLU → Dropout → Linear）进行有病/无病预测。
    *   **决策融合 (Decision Fusion)**：各视角网络先独立输出分类 logits，随后通过**可学习权重** (`nn.Parameter`) 经 Softmax 加权融合各视角结果。
    *   **注意力融合 (Attention Fusion)**：用**可学习注意力池化**替代平均池化，自动聚焦关键切片；同时引入 **Cross-View Attention**（跨视角 Transformer 自注意力），让三个视角在融合前互相交换信息，再送入增强版分类头。

### 3. 注意力融合实现细节

注意力融合通过独立的 `MultiViewAttentionClassifier` 类实现（位于 `src/model.py`），包含三个新模块：

#### Attention Pooling (`src/attention_pooling.py`)
*   用两层 MLP（`Linear → Tanh → Linear(1)`）为每张切片打分，经 Softmax 转为权重后加权求和。
*   输入 `(B, S, 512)` → 输出 `(B, 512)`，替代原来的 `mean(dim=1)`。
*   支持 `share_backbone` 模式下共享或独立的注意力池化层。

#### Cross-View Attention (`src/cross_view_attention.py`)
*   基于 `nn.TransformerEncoder`，使用 **Pre-LN**（`norm_first=True`）架构 + **GELU** 激活函数。
*   Feed-forward 维度为 `feature_dim × 2`（即 1024）。
*   包含残差连接：`output = LayerNorm(transformer_output + input)`。
*   输入 `(B, 3, 512)` → 输出 `(B, 3, 512)`，让三个视角互相"对照着看"。
*   可通过 `cross_view_heads`（默认 8）和 `cross_view_layers`（默认 2）配置。

#### 增强版分类头
*   对比特征融合的 `ReLU + Dropout` 分类头，注意力模式使用 `LayerNorm → Linear → GELU → Dropout → Linear`，提供更稳定的梯度和更强的表达能力。

### 4. 训练策略与数据工程
*   **动态切片与归一化**：支持对 CT 原片进行 HU 窗宽窗位裁剪、边界切片忽略 (`trim_edge_slices`) 以及统一 resize 处理。
*   **类别不平衡处理**：支持通过 `class_weight` 根据训练集的真实类别频次自动加权交叉熵损失，应对实际医疗数据中常见的阳性/阴性比例失调。
*   **自动化验证机制**：支持自动化按标签分布进行 `train/val` 划分，并将验证集的 `AUC`（优先）或 `Accuracy` 作为保留最佳模型权重的指标。

## 三、 三种融合模式使用方法

通过配置文件中的 `model.fusion_type` 切换模式，或直接使用对应的配置文件：

| 融合模式 | `fusion_type` | 切片聚合 | 视角融合 | 分类头 | 运行命令 |
|----------|---------------|----------|----------|--------|----------|
| 特征融合 | `feature` | 平均池化 | 拼接 (Concat) | ReLU MLP | `python train.py --config configs/default.yaml` |
| 决策融合 | `decision` | 平均池化 | 各视角独立分类 → 加权投票 | 每视角独立 MLP | 在 yaml 中设 `fusion_type: decision` |
| **注意力融合** | `attention` | **可学习注意力池化** | **Cross-View Attention** + 拼接 | **LayerNorm + GELU MLP** | `python train.py --config configs/attention_fusion.yaml` |

### 注意力融合模式额外配置项

```yaml
model:
  fusion_type: attention       # 切换为注意力融合
  cross_view_heads: 8          # Cross-View Attention 的注意力头数（默认 8）
  cross_view_layers: 2         # Cross-View Attention 的 Transformer 层数（默认 2）
```

### 数据流对比

```
特征融合:   ResNet18 → mean pooling → cat(3视角) → ReLU MLP → logits
决策融合:   ResNet18 → mean pooling → 各视角独立 MLP → softmax 加权投票 → logits
注意力融合: ResNet18 → Attention Pooling → stack(3视角) → Cross-View Attention → GELU MLP → logits
                        ↑ 自动聚焦关键切片                   ↑ 视角间信息交互
```

## 四、 架构升级路线 (改进.md)

项目已制定分步升级计划，按以下顺序逐步实施（每步验证后再推进下一步）：

| 步骤 | 改动 | 状态 |
|------|------|------|
| Step 1 | Attention Pooling + Cross-View Attention | ✅ **已完成** |
| Step 2 | Slice Transformer / BiGRU 序列建模 | 🔲 待实施 |
| Step 3 | Patient-Level Gated Fusion Head | 🔲 待实施 |
| Step 4 | 训练策略增强（Scheduler / EarlyStopping / AMP / 数据增强） | 🔲 待实施 |

详细设计文档见 `改进.md`。

## 五、 目前获取的成果

1.  **打通端到端医学影像处理 Pipeline**
    框架已经实现了从多种杂乱无章的医学影像文件格式（`png/jpg/bmp/dcm` 的散装切片，或 `nii/nii.gz` 完整3D文件）到直接吐出二分类预测指标的完整自动化流程。
2.  **构建了通用且灵活的配置体系**
    沉淀出了 `default`（轻量/CPU跑通）、`default_decision`（决策融合）、`paper_baseline`（对照论文实验）、`realdata`（面向真实大尺寸数据）和 `attention_fusion`（注意力融合）等多套开箱即用的 YAML 配置文件。
3.  **可对比的多路径融合基线**
    通过特征融合、决策融合、注意力融合三个选项的实现，为进一步的研究和诊断依据留出了对比实验的基础，能够有效衡量每个切片视角对最终疾病判断的贡献度。
4.  **完成第一阶段架构升级（注意力融合）**
    新增 `AttentionPooling` 和 `CrossViewAttention` 两个独立模块，以及 `MultiViewAttentionClassifier` 模型类，实现了 `改进.md` 中 Step 1 的全部目标。代码通过 demo 数据验证，训练流程正常。
5.  **提供高鲁棒性的项目脚手架**
    提供了一个对于新手和实际落地都非常友好的起步版本，能够稳定记录每个 epoch 数据、生成 `best.pt`、防止同病人数据在训练测试集泄露，并为接下来的 Step 2~4 升级铺平了道路。