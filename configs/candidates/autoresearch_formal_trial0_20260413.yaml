# Candidate formal confirmation config preserved from the training branch.
# It keeps the proxy winner hyperparameters separate from the official formal config.
experiment_name: autoresearch_formal_trial0_20260413_mainsync
output_dir: runs/candidates/autoresearch_formal_trial0_20260413_mainsync
seed: 42

data:
  csv_path: data/realdata/metadata.csv
  base_dir: .
  image_size: 512
  num_slices_per_view: 16
  trim_edge_slices: 1
  batch_size: 4
  num_workers: 4
  val_ratio: 0.25
  hu_min: -1000
  hu_max: 1000

model:
  backbone: resunet
  fusion_type: decision
  share_backbone: false
  use_pretrained: true
  freeze_layers: 3
  fusion_hidden_dim: 256
  dropout: 0.3
  use_attention_pooling: true

train:
  epochs: 15
  lr: 0.00005
  weight_decay: 0.001
  class_weight: true
  device: auto
  augmentation: true
  label_smoothing: 0.0
  gradient_clip_norm: 1.0
