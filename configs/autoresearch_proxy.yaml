# Fast proxy config for autoresearch-style iteration.
# Keep this fixed during proxy experimentation unless the change itself is the experiment.
experiment_name: autoresearch_proxy
output_dir: runs/autoresearch_proxy
seed: 42

data:
  csv_path: data/realdata/metadata.csv
  base_dir: .
  image_size: 192
  num_slices_per_view: 16
  trim_edge_slices: 1
  batch_size: 2
  num_workers: 0
  val_ratio: 0.25
  hu_min: -1000
  hu_max: 1000

model:
  backbone: resnet18
  fusion_type: attention
  share_backbone: true
  use_pretrained: true
  fusion_hidden_dim: 384
  dropout: 0.3
  cross_view_heads: 4
  cross_view_layers: 2

train:
  epochs: 4
  lr: 0.0001
  weight_decay: 0.0001
  class_weight: true
  device: auto

